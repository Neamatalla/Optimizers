# Backlog

Known issues / improvements not yet worth blocking on, found during real usage.

(2026-09-09: the PageSpeed Insights integration — `pagespeed.ts`, its API key rotation, and
the site-wide sweep — is now actually gone: the file is deleted, the PageSpeed category and
its PS-1..PS-4 checks are gone, and what they scored lives in WEB-60..WEB-63, measured from
the headless browser instead. `discover-pages.ts` survives — the browser still needs real
cart/product URLs to navigate to.)
