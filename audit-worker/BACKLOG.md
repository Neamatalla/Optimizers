# Backlog

Known issues / improvements not yet worth blocking on, found during real usage.

(2026-09-06: the PageSpeed Insights integration — `pagespeed.ts`, its API key rotation, and
the site-wide sweep — was removed from the audit entirely; the one open item that used to be
tracked here about it no longer applies.)
