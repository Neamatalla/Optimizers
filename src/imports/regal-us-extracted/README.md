
  # Create HTML CSS Layout

  This is a code bundle for Create HTML CSS Layout. The original project is available at https://www.figma.com/design/UNGaYiq4naPQj582256dZS/Create-HTML-CSS-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  